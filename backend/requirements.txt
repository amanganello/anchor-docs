# LEGACY PLACEHOLDER — do not use for installation.
#
# This file predates the uv/pyproject.toml design and lists stale, incorrect
# dependencies (including openai, which is not used). It will be deleted when
# pyproject.toml and uv.lock are scaffolded in Phase 1 implementation.
#
# The authoritative dependency manifest is pyproject.toml (to be created).
# Install with: uv sync --frozen
#
# Planned runtime dependencies (see backend Phase 1 design spec):
#   fastapi
#   uvicorn[standard]
#   google-genai
#   pydantic-settings
#
# Dev dependencies:
#   pytest
#   pytest-asyncio
#   httpx
